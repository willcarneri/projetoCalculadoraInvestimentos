function convertToMonthlyReturnRate(yearlyRetunrRate) {
  return yearlyRetunrRate ** (1 / 2);
}

function generateReturnsArray(
  startingAmount = 0,
  timeHorizon = 0,
  timePeriod = 'monthly',
  montlyContribution = 0,
  returnRate = 0,
  returnTimeFrame = 'monthly'
) {}
